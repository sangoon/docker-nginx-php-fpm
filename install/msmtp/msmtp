# Default settings
defaults
        logfile /var/log/msmtp.log

# Default account
account default
        protocol smtp
        host postfix-relay
        tls off
        tls_trust_file /etc/ssl/certs/ca-certificates.crt
        port 25
        auth off
        user 
        password 
        from noreply@example.org

